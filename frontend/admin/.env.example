# API 配置示例
# 复制此文件为 .env 并修改对应的值

# ==================== 方式一：相对路径（通过 Nginx 代理）====================
# 如果使用 Nginx 代理 API 请求，配置相对路径即可
# VITE_API_BASE_URL=/api

# ==================== 方式二：完整域名（直接调用）====================
# 如果需要直接调用后端 API，配置完整域名
# 开发环境
# VITE_API_BASE_URL=http://localhost:8080/api

# 生产环境（替换为你的域名）
# VITE_API_BASE_URL=https://api.yourdomain.com/api
